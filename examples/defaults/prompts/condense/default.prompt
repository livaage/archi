# Default condense prompt
# Used to condense a chat history and follow-up question into a standalone question.
#
# Required template variables:
#   {history} - The chat history
#   {question} - The follow-up question
#
Given the following conversation between you (the AI assistant) and a human user, and a follow up question, rephrase the follow up question to be a standalone question in its original language.

Chat History: {history}
Follow Up Input: {question}
Standalone question:
